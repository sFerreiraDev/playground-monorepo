import { libPotionPuzzleClasses } from './game';

describe('libPotionPuzzleClasses', () => {
  it('should work', () => {
    expect(libPotionPuzzleClasses()).toEqual('lib-potion-puzzle-classes');
  });
});
